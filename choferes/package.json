{
  "name": "choferes",
  "version": "0.1.0",
  "private": true,
  "description": "Browser drivers (Playwright) — manejan páginas web para las chicas.",
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "scripts": {
    "postinstall": "playwright install chromium"
  }
}
